# 데이터

## 정보 단위

**비트**: 0과 1을 나타내는 가장 작은 정보 단위이다. n비트는 2<sup>n</sup>개의 정보를 표현할 수 있다.

**바이트**: 여덟 개의 비트를 묶은 단위이다.

## 이진법

0과 1만으로 모든 숫자를 표현하는 방법이다.

## 워드

CPU가 한 번에 처리할 수 있는 데이터 크기를 의미한다. CPU가 16비트를 한 번에 처리할 수 있다면 1워드는 16비트가 된다.

워드의 절반 크기를 하프 워드, 1배 크기를 풀 워드, 2배 크기를 더블 워드라고 부른다.

### 이진수의 음수 표현

`2의 보수`를 구해 값을 음수로 간주한다.

2의 보수의 사전적 의미는 어떤 수를 그보다 큰 2<sup>n</sup>에서 뺀 값을 의미한다.

<img width="528" height="504" alt="2의 보수" src="https://github.com/user-attachments/assets/591807fa-db23-4fc3-9c49-f44d6f28b68f" />

사전적으로 어렵게 이해할 필요 없이 2의 보수를 쉽게 표현하면 모든 0과 1을 뒤집고 1을 더한 값으로 이해하면 된다.

하지만 -1011<sub>(2)</sub>를 표현하기 위한 음수로서의 0101<sub>(2)</sub>와 십진수 5를 표현하기 위한 양수로서의 0101<sub>(2)</sub>을 구분하기 위해서는 어떻게 해야할까?

위와 같은 경우에는 `플래그`를 사용해 컴퓨터 내부에서 양수인지 음수인지 구분한다.

## 십육진법

수가 14가 넘어가는 시점에 자리 올림을 하는 숫자 표현 방식이다.

### 십육진수를 이진수로, 이진수를 십육진수로 변환하기

**십육진수를 이진수로 변환하기**

<img width="1084" height="442" alt="이진수로 변환" src="https://github.com/user-attachments/assets/cda83fc6-dbc6-4b9f-930f-c678e0b6b172" />

- 십육진수를 이루는 숫자 하나를 이진수로 표현하기 위해서는 4비트가 필요하다.
- 십육진수를 이진수로 변환하는 가장 간편한 방법은 십육진수 한글자를 4비트의 이진수로 간주하는 것이다.

**이진수를 십육진수로 변환하기**

<img width="502" height="424" alt="십육진수로 변환하기" src="https://github.com/user-attachments/assets/e39bdb5d-2f45-4092-97fc-6822bdea621e" />

- 이진수 숫자 네 개씩 끊고 네 개의 숫자를 하나의 십육진수로 변환한 뒤 그대로 이어 붙이면 된다.

## 문자 집합과 인코딩

**문자 집합**: 컴퓨터가 인식하고 표현할 수 있는 문자의 모음을 말한다.

문자 집합에 속한 문자라고 해서 컴퓨터가 그대로 이해할 수 있는 거싱 아니며 문자를 0과 1로 변환하는 `문자 인코딩` 과정을 거쳐야 컴퓨터가 이해할 수 있다.

문자 코드를 사람이 이해할 수 있는 문자로 변환하는 과정을 `문자 디코딩`이라고 한다.

## 아스키 코드

`아스키`는 초창기 문자 집합 중 하나로, 영어 알파벳과 아라비아 숫자, 일부 특수 문자를 포함한다.

아스키 문자에 대응된 고유한 수를 `아스키 코드`라고 하며 0부터 127까지 128개의 숫자 중 하나의 고유한 수에 대응된다.

## 유니코드와 UTF-8

모든 언어를 아우르는 문자 집합과 통일된 표준 인코딩 방식이 있다면 언어별로 인코딩하는 수고로움을 덜 수 있다. 그래서 `유니코드` 문자 집합이 등장했다.

유니코드를 인코딩하는 방법 중 가장 대중적인 방식은 `UTF-8`이다.

UTF-8은 1바이트부터 4바이트까지의 인코딩 결과를 만들어낸다. UTF-8로 인코딩한 결과가 몇 바이트가 될지는 유니코드 문자에 부여된 값의 범위에 따라 결정된다.

<img width="981" height="622" alt="스크린샷 2026-03-23 오후 3 43 19" src="https://github.com/user-attachments/assets/04a1a054-dbd2-49cd-b481-920f4cd54eb8" />
